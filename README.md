# Attri basic Hostinger test

Minimal Next.js application used only to verify Hostinger's Git deployment workflow.

```bash
npm install
npm run build
npm start
```

Use Node.js 22, `npm ci`, `npm run build`, and `npm start` in Hostinger.
