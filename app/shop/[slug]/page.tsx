import{notFound}from"next/navigation";import{createServerSupabaseClient}from"@/lib/supabase/server";export const dynamic="force-dynamic";export default async function Product({params}:{params:Promise<{slug:string}>}){const{slug}=await params,s=createServerSupabaseClient(),{data}=await s.from("products").select("title,category,description,price_paise,image_path,stock_status").eq("slug",slug).eq("status","published").is("deleted_at",null).maybeSingle();if(!data)notFound();return <main className="project-detail">{data.image_path&&<img src={data.image_path} alt={data.title}/>}<div className="project-detail-copy"><span>{data.category} · {data.stock_status.replace("_"," ")}</span><h1>{data.title}</h1><p>{data.description}</p><h2 style={{color:"#ff9b3d"}}>₹{(data.price_paise/100).toLocaleString("en-IN")}</h2><a href="/contact">Request product guidance →</a></div></main>}
