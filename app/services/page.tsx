import{createServerSupabaseClient}from"@/lib/supabase/server";export const dynamic="force-dynamic";export default async function Services(){const s=createServerSupabaseClient(),{data}=await s.from("services_catalogue").select("id,name,summary").eq("status","published").order("created_at");return <main className="public-catalogue"><header><a href="/">ATTRI ASSOCIATES</a><a href="/access">Client access →</a></header><p>Integrated expertise</p><h1>One vision.<br/><em>Every discipline.</em></h1><section>{data?.length?data.map((x,i)=><article key={x.id}><span>{String(i+1).padStart(2,"0")}</span><div><h2>{x.name}</h2><p>{x.summary||"Details will be shared during your consultation."}</p></div><a href="/consultation">Discuss this service →</a></article>):<article className="catalogue-empty">Our service catalogue is being updated. <a href="/consultation">Book a consultation →</a></article>}</section></main>}
