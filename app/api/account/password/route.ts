import{createServerClient}from"@supabase/ssr";import{NextRequest,NextResponse}from"next/server";import{z}from"zod";const schema=z.object({password:z.string().min(10).max(128).regex(/[a-z]/).regex(/[A-Z]/).regex(/[0-9]/)});export async function PATCH(r:NextRequest){const i=schema.safeParse(await r.json().catch(()=>null));if(!i.success)return NextResponse.json({error:"Use a 10+ character password with uppercase, lowercase and a number."},{status:400});let response=NextResponse.json({ok:true});const s=createServerClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY!,{cookies:{getAll:()=>r.cookies.getAll(),setAll(v){v.forEach(({name,value,options})=>response.cookies.set(name,value,options))}}}),{data:{user}}=await s.auth.getUser();if(!user)return NextResponse.json({error:"Your recovery session has expired. Request another reset link."},{status:401});const{error}=await s.auth.updateUser({password:i.data.password});if(error)return NextResponse.json({error:"Unable to update password. Request a new recovery link."},{status:400});return response}
