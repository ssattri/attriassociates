import{createAuthServerClient}from"@/lib/supabase/auth";import{redirect}from"next/navigation";export const dynamic="force-dynamic";export default async function Account(){const s=await createAuthServerClient(),{data:{user}}=await s.auth.getUser();if(!user)redirect("/access");const{data:p}=await s.from("profiles").select("full_name,workspace_type,consultant_application_status").eq("id",user.id).maybeSingle();return <main className="account-home"><p>My workspace</p><h1>Welcome, {p?.full_name||"Member"}.</h1><span>{p?.workspace_type||"client"} account</span>{p?.workspace_type==="consultant"&&p.consultant_application_status!=="approved"?<h2>Your consultant application is awaiting approval.</h2>:<h2>Your secure workspace is ready. Orders, consultations and project tools will appear here as they become available.</h2>}<form action="/api/account/logout" method="post"><button>Sign out</button></form><a href="/">Return to website →</a></main>}
