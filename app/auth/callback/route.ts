import{createServerClient}from"@supabase/ssr";import{NextRequest,NextResponse}from"next/server";export async function GET(r:NextRequest){const url=new URL(r.url),code=url.searchParams.get("code"),next=url.searchParams.get("next")||"/account";if(!code||!next.startsWith("/"))return NextResponse.redirect(new URL("/access",r.url));let response=NextResponse.redirect(new URL(next,r.url));const s=createServerClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY!,{cookies:{getAll:()=>r.cookies.getAll(),setAll(v){v.forEach(({name,value,options})=>response.cookies.set(name,value,options))}}});const{error}=await s.auth.exchangeCodeForSession(code);if(error)return NextResponse.redirect(new URL("/access?message=link-expired",r.url));return response}
