import{notFound}from"next/navigation";import{createServerSupabaseClient}from"@/lib/supabase/server";export const dynamic="force-dynamic";export default async function Project({params}:{params:Promise<{slug:string}>}){const{slug}=await params,s=createServerSupabaseClient(),{data}=await s.from("projects").select("title,category,location,summary,body,featured_image_path,gallery").eq("slug",slug).eq("status","published").is("deleted_at",null).maybeSingle();if(!data)notFound();const text=Array.isArray(data.body)?data.body.map((x:{value?:string})=>x.value||"").join("\n\n"):"";return <main className="project-detail">{data.featured_image_path&&<img src={data.featured_image_path} alt={data.title}/>}<div className="project-detail-copy"><span>{data.category}{data.location?` · ${data.location}`:""}</span><h1>{data.title}</h1><p>{data.summary}</p>{text&&<div style={{whiteSpace:"pre-line"}}>{text}</div>}<a href="/consultation">Discuss a similar project →</a></div></main>}
