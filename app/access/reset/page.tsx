"use client";import{useState}from"react";export default function Reset(){const[p,setP]=useState(""),[confirm,setConfirm]=useState(""),[m,setM]=useState(""),[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();if(p!==confirm){setM("Passwords do not match.");return}setBusy(true);const r=await fetch("/api/account/password",{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({password:p})}),x=await r.json().catch(()=>({}));setM(r.ok?"Password updated. You can now sign in.":x.error||"Could not update password.");setBusy(false);if(r.ok)setTimeout(()=>location.assign("/access"),1200)}return <main className="account-standalone"><form onSubmit={submit}><p>Secure recovery</p><h1>Create a new<br/><em>password.</em></h1><h2>Use 10 or more characters with uppercase, lowercase and a number.</h2><label>New password<input type="password" value={p} onChange={e=>setP(e.target.value)} required minLength={10} autoComplete="new-password"/></label><label>Confirm password<input type="password" value={confirm} onChange={e=>setConfirm(e.target.value)} required minLength={10} autoComplete="new-password"/></label><button disabled={busy}>{busy?"Updating…":"Update password →"}</button>{m&&<small>{m}</small>}<a href="/access">← Back to sign in</a></form></main>}
