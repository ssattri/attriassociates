"use client";import{useState}from"react";export default function Forgot(){const[email,setEmail]=useState(""),[m,setM]=useState(""),[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);const r=await fetch("/api/account/password-reset",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email})});setM(r.ok?"If an account exists, a secure recovery link has been sent to that email address.":"Enter a valid email address.");setBusy(false)}return <main className="account-standalone"><form onSubmit={submit}><p>Account recovery</p><h1>Reset your<br/><em>password.</em></h1><h2>Enter your email and we will send a secure, time-limited recovery link.</h2><label>Email address<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required autoComplete="email"/></label><button disabled={busy}>{busy?"Sending…":"Send recovery link →"}</button>{m&&<small>{m}</small>}<a href="/access">← Back to sign in</a></form></main>}
