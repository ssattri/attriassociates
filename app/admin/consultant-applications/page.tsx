import{createAuthServerClient}from"@/lib/supabase/auth";import{ConsultantApplications}from"@/components/consultant-applications";export const dynamic="force-dynamic";export default async function ConsultantApplicationsPage(){const s=await createAuthServerClient(),{data}=await s.from("profiles").select("id,full_name,phone,consultant_application_status,created_at").eq("workspace_type","consultant").order("created_at",{ascending:false});return <ConsultantApplications applications={data||[]}/>}
