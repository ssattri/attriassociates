import{createAuthServerClient}from"@/lib/supabase/auth";import{SettingsWorkspace}from"@/components/settings-workspace";export const dynamic="force-dynamic";export default async function Settings(){const s=await createAuthServerClient(),{data}=await s.from("site_settings").select("value").eq("key","identity_contact").maybeSingle();return <SettingsWorkspace initial={data?.value||{}}/>}
