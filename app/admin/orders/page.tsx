import{createAuthServerClient}from"@/lib/supabase/auth";import{OrdersWorkspace}from"@/components/orders-workspace";export const dynamic="force-dynamic";export default async function Orders(){const s=await createAuthServerClient(),{data}=await s.from("orders").select("id,reference,customer_name,customer_email,total_paise,payment_status,fulfillment_status,created_at").order("created_at",{ascending:false});return <OrdersWorkspace orders={data||[]}/>}
