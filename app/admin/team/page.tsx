import{createAuthServerClient}from"@/lib/supabase/auth";import{TeamWorkspace}from"@/components/team-workspace";export const dynamic="force-dynamic";export default async function Team(){const s=await createAuthServerClient(),{data}=await s.from("profiles").select("id,full_name,phone,role,account_status,created_at").order("created_at",{ascending:false});return <TeamWorkspace members={data||[]}/>}
