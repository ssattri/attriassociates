import{createAuthServerClient}from"@/lib/supabase/auth";export const dynamic="force-dynamic";export default async function Notifications(){const s=await createAuthServerClient();const[{data:logs},{count:pendingConsultants},{count:newEnquiries}]=await Promise.all([s.from("audit_logs").select("id,action,entity_type,entity_id,created_at").order("created_at",{ascending:false}).limit(40),s.from("profiles").select("id",{count:"exact",head:true}).eq("workspace_type","consultant").eq("consultant_application_status","pending"),s.from("enquiries").select("id",{count:"exact",head:true}).eq("status","new")]);return <main className="crm-page notification-page"><header className="crm-top"><a className="crm-back" href="/admin">← Control centre</a><div><p>Operations / notifications</p><h1>Attention centre</h1></div></header><section className="notification-alerts"><a href="/admin/consultant-applications"><b>{pendingConsultants||0}</b><span>Consultant applications<br/>awaiting review</span><i>→</i></a><a href="/admin/enquiries"><b>{newEnquiries||0}</b><span>New service enquiries<br/>awaiting action</span><i>→</i></a></section><section className="analytics-panel"><header><div><p>System activity</p><h2>Audit log</h2></div><span>Last 40 events</span></header>{logs?.length?logs.map(log=><div key={log.id}><span><b>{log.action.replaceAll("_"," ").replaceAll("."," · ")}</b><small>{log.entity_type} · {new Date(log.created_at).toLocaleString("en-IN")}</small></span><em>{log.entity_id?.slice(0,8)||"System"}</em></div>):<p className="crm-empty">No recorded activity yet.</p>}</section></main>}
