import{createAuthServerClient}from"@/lib/supabase/auth";import{CheckoutWorkspace}from"@/components/checkout-workspace";export const dynamic="force-dynamic";export default async function Q(){const s=await createAuthServerClient(),{data}=await s.from("checkout_requests").select("id,reference,name,email,phone,cart,total_paise,status").order("created_at",{ascending:false});return <CheckoutWorkspace requests={data||[]}/>}
