import{createAuthServerClient}from"@/lib/supabase/auth";import{PageEditor}from"@/components/page-editor";export const dynamic="force-dynamic";export default async function E({params}:{params:Promise<{id:string}>}){const{id}=await params,s=await createAuthServerClient(),{data}=await s.from("pages").select("id,title,slug,excerpt,body,seo_title,meta_description,status").eq("id",id).maybeSingle();return data?<PageEditor page={data}/>:<main className="crm-page">Page not found.</main>}
