"use client";import{useState}from"react";export function CheckoutRequestForm({cart}:{cart:any[]}){const[s,setS]=useState("idle"),[ref,setRef]=useState("");async function send(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setS("sending");const body={...Object.fromEntries(new FormData(e.currentTarget)),cart};const r=await fetch("/api/checkout-requests",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body)}),x=await r.json().catch(()=>({}));if(r.ok){setRef(x.reference);setS("sent");localStorage.removeItem("attri-cart")}else setS("error")}if(s==="sent")return <p>Request {ref} received. Our team will contact you with payment and delivery confirmation.</p>;return <form className="booking-form" onSubmit={send}><label>Name<input name="name" required/></label><label>Email<input name="email" type="email" required/></label><label>Phone<input name="phone" required/></label><label className="wide">Delivery address<textarea name="address" required minLength={8}/></label><button disabled={s==="sending"}>{s==="sending"?"Sending…":"Request secure checkout →"}</button>{s==="error"&&<p>Unable to send request. Please try later.</p>}</form>}
