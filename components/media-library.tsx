"use client";import{useState}from"react";export function MediaLibrary(){const[m,setM]=useState(""),[url,setUrl]=useState("");async function upload(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setM("Uploading…");const r=await fetch("/api/admin/media",{method:"POST",body:new FormData(e.currentTarget)}),x=await r.json().catch(()=>({}));if(r.ok){setUrl(x.url);setM("Image uploaded. Copy the public URL below.")}else setM(x.error||"Upload failed.")}return <main className="crm-page"><header className="crm-top"><a className="crm-back" href="/admin">← Control centre</a><div><p>Content / media</p><h1>Media library</h1></div></header><form className="booking-form" onSubmit={upload}><label className="wide">Upload image<input name="file" type="file" accept="image/jpeg,image/png,image/webp,image/avif" required/></label><p>JPEG, PNG, WebP or AVIF · maximum 10 MB</p><button>Upload image →</button>{m&&<p>{m}</p>}</form>{url&&<section className="crm-table-card" style={{marginTop:24,padding:24}}><img src={url} alt="Uploaded media" style={{maxWidth:"100%",maxHeight:360}}/><label style={{display:"block",marginTop:16}}>Public image URL<input readOnly value={url} onFocus={e=>e.currentTarget.select()} style={{width:"100%",padding:12,marginTop:8}}/></label></section>}</main>}
