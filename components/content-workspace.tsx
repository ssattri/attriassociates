"use client";import{useState}from"react";import{useRouter}from"next/navigation";type P={id:string;title:string;slug:string;excerpt:string|null;status:string;updated_at:string};export function ContentWorkspace({pages}:{pages:P[]}){const r=useRouter();const[f,setF]=useState({title:"",slug:"",excerpt:"",status:"draft"});const[m,setM]=useState("");async function save(e:React.FormEvent){e.preventDefault();const x=await fetch("/api/admin/pages",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(f)});setM(x.ok?"Page created.":"Could not create page. Check the URL slug is unique.");if(x.ok){setF({title:"",slug:"",excerpt:"",status:"draft"});r.refresh()}}return <main className="crm-page"><header className="crm-top"><a className="crm-back" href="/admin">← Control centre</a><div><p>Content / pages</p><h1>Website content</h1></div></header><section className="crm-table-card">{pages.map(p=><div className="crm-row" key={p.id}><span><b>{p.title}</b><small>/{p.slug}</small></span><span>{p.excerpt||"No summary"}</span><span className={`crm-status ${p.status}`}>{p.status}</span><span>{new Date(p.updated_at).toLocaleDateString("en-IN")}</span></div>)}{!pages.length&&<div className="crm-empty">No CMS pages yet.</div>}</section><form className="booking-form" style={{marginTop:25}} onSubmit={save}><label>Page title<input value={f.title} onChange={e=>setF({...f,title:e.target.value,slug:f.slug||e.target.value.toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/(^-|-$)/g,"")})} required/></label><label>URL slug<input value={f.slug} onChange={e=>setF({...f,slug:e.target.value})} required/></label><label className="wide">Short description<textarea value={f.excerpt} onChange={e=>setF({...f,excerpt:e.target.value})}/></label><label>Publishing status<select value={f.status} onChange={e=>setF({...f,status:e.target.value})}><option value="draft">Draft</option><option value="published">Published</option><option value="archived">Archived</option></select></label><button>Create page →</button>{m&&<p>{m}</p>}</form></main>}
