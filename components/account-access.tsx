"use client";import{useState}from"react";type Mode="login"|"signup";type Kind="client"|"consultant";export function AccountAccess(){const[mode,setMode]=useState<Mode>("login"),[kind,setKind]=useState<Kind>("client"),[message,setMessage]=useState(""),[busy,setBusy]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setMessage("");const form=new FormData(e.currentTarget),data=Object.fromEntries(form);data.workspace_type=kind;const r=await fetch(`/api/account/${mode}`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(data)}),x=await r.json().catch(()=>({}));if(r.ok){if(mode==="login")location.assign("/account");else setMessage(x.confirmationRequired?"Account created. Please confirm your email before signing in.":kind==="consultant"?"Application received. We will review your consultant access.":"Account created. You can now sign in.");}else setMessage(x.error||"Please try again.");setBusy(false)}return <main className="account-access"><section className="account-identity"><a className="account-wordmark" href="/"><span>↗</span><b>ATTRI<small>ASSOCIATES</small></b></a><div><p>One secure account</p><h1>Access your<br/><em>workspace.</em></h1><h2>Sign in or register directly with Attri Associates as a Client or Consultant.</h2><aside><span>Secure sign-in</span><span>Direct registration</span><span>Role-based access</span></aside></div></section><section className="account-pane"><form className="account-card" onSubmit={submit}><nav><button type="button" className={mode==="login"?"active":""} onClick={()=>{setMode("login");setMessage("")}}>Sign in</button><button type="button" className={mode==="signup"?"active":""} onClick={()=>{setMode("signup");setMessage("")}}>Create account</button></nav><p className="account-eyebrow">{mode==="login"?"Welcome back":"Create your account"}</p><h3>{mode==="login"?"Choose your login type":"Choose your workspace"}</h3><p className="account-copy">{mode==="login"?"Select your workspace and enter your account details.":"Client accounts are ready after email confirmation. Consultant access is reviewed by our team."}</p><div className="account-kinds"><button type="button" className={kind==="client"?"selected":""} onClick={()=>setKind("client")}><b>Client <i>01</i></b><span>Shop, courses, services and consultations</span></button><button type="button" className={kind==="consultant"?"selected":""} onClick={()=>setKind("consultant")}><b>Consultant <i>02</i></b><span>Vastu Compass, projects and reports</span></button></div>{mode==="signup"&&<><label>Full name<input name="full_name" required maxLength={100}/></label><label>Phone number<input name="phone" type="tel" required maxLength={40}/></label></>}<label>Email address<input name="email" type="email" placeholder="name@example.com" required autoComplete="email"/></label><label>Password<input name="password" type="password" placeholder="10+ characters, uppercase, lowercase and number" required minLength={10} autoComplete={mode==="login"?"current-password":"new-password"}/></label><button className="account-submit" disabled={busy}>{busy?"Please wait…":mode==="login"?`Sign in as ${kind} →`:`Create ${kind} account →`}</button><small className="account-note">Secure session · Encrypted password · Role-based access</small>{mode==="login"&&<a className="account-forgot" href="/access/forgot">Forgot password?</a>}{message&&<p className="account-message">{message}</p>}<a href="/">← Return to website</a></form></section></main>}
