"use client";
import { useState } from "react";
export function BookingForm() { const [state,setState]=useState("idle"); async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setState("sending");const r=await fetch("/api/consultations",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(Object.fromEntries(new FormData(e.currentTarget)))});setState(r.ok?"sent":"error");if(r.ok)e.currentTarget.reset();} return <form className="booking-form" onSubmit={submit}><label>Name<input name="name" required minLength={2}/></label><label>Email<input name="email" type="email" required/></label><label>Phone<input name="phone" required minLength={7}/></label><label>Consultation<select name="consultation_type"><option>Scientific Vastu consultation</option><option>Architecture consultation</option><option>Design & planning consultation</option></select></label><label>Preferred date<input name="preferred_date" type="date"/></label><label>Preferred time<select name="preferred_time"><option>Morning (10 AM–1 PM)</option><option>Afternoon (2 PM–5 PM)</option><option>Evening (5 PM–7 PM)</option></select></label><label className="wide">Project details<textarea name="notes"/></label><button disabled={state==="sending"}>{state==="sending"?"Sending…":"Request consultation →"}</button>{state==="sent"&&<p>Thank you. We will confirm your consultation shortly.</p>}{state==="error"&&<p>Please call +91 99907 77716 while booking is being enabled.</p>}</form>; }
