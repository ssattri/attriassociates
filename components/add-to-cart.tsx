"use client";import{useState}from"react";export function AddToCart({item}:{item:{slug:string;title:string;price:number}}){const[s,setS]=useState("Add to cart");function add(){const c=JSON.parse(localStorage.getItem("attri-cart")||"[]"),x=c.find((i:any)=>i.slug===item.slug);if(x)x.quantity++;else c.push({...item,quantity:1});localStorage.setItem("attri-cart",JSON.stringify(c));setS("Added ✓");setTimeout(()=>setS("Add to cart"),1500)}return <button className="solid-button" onClick={add}>{s} →</button>}
