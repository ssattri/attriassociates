"use client";import{useState}from"react";export function IntegrationWorkspace(){const[f,setF]=useState({keyId:"",keySecret:"",webhookSecret:""}),[m,setM]=useState("");async function save(e:React.FormEvent){e.preventDefault();const r=await fetch("/api/admin/integrations/razorpay",{method:"PUT",headers:{"content-type":"application/json"},body:JSON.stringify(f)});setM(r.ok?"Razorpay credentials encrypted and saved.":"Unable to save. Complete server vault setup first.")}return <main className="crm-page"><header className="crm-top"><a className="crm-back" href="/admin">← Control centre</a><div><p>System / integrations</p><h1>API keys & integrations</h1></div></header><form className="booking-form" onSubmit={save}><label>Razorpay Key ID<input value={f.keyId} onChange={e=>setF({...f,keyId:e.target.value})} placeholder="rzp_test_..." required/></label><label>Razorpay Key Secret<input type="password" value={f.keySecret} onChange={e=>setF({...f,keySecret:e.target.value})} required/></label><label className="wide">Razorpay Webhook Secret<input type="password" value={f.webhookSecret} onChange={e=>setF({...f,webhookSecret:e.target.value})} required/></label><p>Secrets are encrypted server-side and are never displayed again after saving.</p><button>Encrypt & save Razorpay →</button>{m&&<p>{m}</p>}</form></main>}
