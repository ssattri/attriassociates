"use client";import{useState}from"react";const fields=[['business_name','Business name'],['tagline','Tagline'],['primary_email','Primary email'],['secondary_email','Secondary email'],['primary_phone','Primary mobile'],['secondary_phone','Secondary mobile'],['whatsapp_number','WhatsApp number'],['emergency_phone','Emergency consultation number'],['address','Office address'],['hours','Hours & availability'],['facebook','Facebook URL'],['instagram','Instagram URL'],['youtube','YouTube URL'],['linkedin','LinkedIn URL']];export function SettingsWorkspace({initial}:{initial:any}){const[f,setF]=useState(initial),[m,setM]=useState('');async function save(e:React.FormEvent){e.preventDefault();const r=await fetch('/api/admin/settings/identity-contact',{method:'PUT',headers:{'content-type':'application/json'},body:JSON.stringify(f)});setM(r.ok?'Business information saved and applied to the public site.':'Could not save settings.')}return <main className="crm-page"><header className="crm-top"><a className="crm-back" href="/admin">← Control centre</a><div><p>System / configuration</p><h1>Settings & integrations</h1></div></header><div className="settings-layout"><aside><a className="active" href="#identity">Identity & contact<small>Business details</small></a><a href="#address">Addresses<small>Office location</small></a><a href="#hours">Hours & availability<small>Customer hours</small></a><a href="#social">Social profiles<small>Public channels</small></a><a href="/admin/integrations">API keys & integrations<small>Super admin only</small></a></aside><form className="settings-card" id="identity" onSubmit={save}><div><h2>Identity & contact</h2><p>These values are editable by authorised administrators and displayed dynamically on the frontend.</p></div><div className="settings-fields">{fields.map(([key,label])=><label key={key}>{label}<input value={f[key]||''} onChange={e=>setF({...f,[key]:e.target.value})}/><small>{key}</small></label>)}</div><button>Save business information</button>{m&&<p>{m}</p>}</form></div></main>}
