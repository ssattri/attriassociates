"use client";import{useState}from"react";export function PaymentLinkButton({id}:{id:string}){const[s,setS]=useState("Generate payment link");async function go(){setS("Generating…");const r=await fetch(`/api/admin/checkout-requests/${id}/payment-link`,{method:"POST"}),x=await r.json().catch(()=>({}));if(r.ok){navigator.clipboard?.writeText(x.url);setS("Link copied ✓")}else setS(x.error||"Try again")}return <button onClick={go}>{s}</button>}
