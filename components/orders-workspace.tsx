"use client";import{useState}from"react";import{useRouter}from"next/navigation";export function OrdersWorkspace({orders}:{orders:any[]}){const r=useRouter(),[items,setItems]=useState(orders);async function update(id:string,fulfillment_status:string){const q=await fetch(`/api/admin/orders/${id}`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({fulfillment_status})});if(q.ok){setItems(x=>x.map((v:any)=>v.id===id?{...v,fulfillment_status}:v));r.refresh()}}return <main className="crm-page"><header className="crm-top"><a className="crm-back" href="/admin">← Control centre</a><div><p>Commerce / fulfilment</p><h1>Orders</h1></div></header><section className="crm-table-card">{items.map((o:any)=><div className="crm-row" key={o.id}><span><b>{o.customer_name}</b><small>{o.reference} · {o.customer_email}</small></span><span>₹{(o.total_paise/100).toLocaleString("en-IN")}</span><span className="crm-status converted">{o.payment_status}</span><select value={o.fulfillment_status} onChange={e=>update(o.id,e.target.value)}><option>pending</option><option>processing</option><option>shipped</option><option>completed</option><option>cancelled</option></select></div>)}{!items.length&&<div className="crm-empty">Paid orders will appear here automatically.</div>}</section></main>}
