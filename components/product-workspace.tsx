"use client";import{useState}from"react";import{useRouter}from"next/navigation";type P={id:string;title:string;slug:string;category:string;description:string;price_paise:number;status:string;stock_status:string};export function ProductWorkspace({products}:{products:P[]}){const r=useRouter(),[f,setF]=useState({title:"",slug:"",category:"Vastu remedy",description:"",price_paise:0,status:"draft"}),[m,setM]=useState("");async function save(e:React.FormEvent){e.preventDefault();const q=await fetch("/api/admin/products",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(f)});setM(q.ok?"Product saved.":"Could not save product.");if(q.ok){setF({...f,title:"",slug:"",description:""});r.refresh()}}return <main className="crm-page"><header className="crm-top"><a className="crm-back" href="/admin">← Control centre</a><div><p>Commerce / shop</p><h1>Product manager</h1></div><a className="crm-create" href="/shop" target="_blank">View shop</a></header><section className="crm-table-card">{products.map(p=><div className="crm-row" key={p.id}><span><b>{p.title}</b><small>{p.category}</small></span><span>₹{(p.price_paise/100).toLocaleString("en-IN")}</span><span className={`crm-status ${p.status}`}>{p.status}</span><span>{p.stock_status.replace("_"," ")}</span></div>)}{!products.length&&<div className="crm-empty">No products yet.</div>}</section><form className="booking-form" style={{marginTop:25}} onSubmit={save}><label>Product title<input value={f.title} onChange={e=>setF({...f,title:e.target.value,slug:f.slug||e.target.value.toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/(^-|-$)/g,"")})} required/></label><label>URL slug<input value={f.slug} onChange={e=>setF({...f,slug:e.target.value})} required/></label><label>Category<input value={f.category} onChange={e=>setF({...f,category:e.target.value})}/></label><label>Price (₹)<input type="number" min="0" value={f.price_paise/100} onChange={e=>setF({...f,price_paise:Math.round(Number(e.target.value)*100)})}/></label><label className="wide">Description<textarea value={f.description} onChange={e=>setF({...f,description:e.target.value})}/></label><label>Status<select value={f.status} onChange={e=>setF({...f,status:e.target.value})}><option>draft</option><option>published</option><option>archived</option></select></label><button>Save product →</button>{m&&<p>{m}</p>}</form></main>}
